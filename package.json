{
  "name": "docswell-ds",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @tailwindcss/cli -i ./styles/app.css -o ./styles/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./styles/app.css -o ./styles/output.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "docswell-ds — a startup design system built the modern way",
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
